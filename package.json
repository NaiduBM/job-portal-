{
  "name": "job-portal",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://<NaiduBM>.github.io/<job-portal->",  // Replace with your GitHub username and repo name
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    // Your dependencies here
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"  // Ensure this is listed
  }
}
